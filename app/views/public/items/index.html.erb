<table>
    <tr>
      <th>ジャンル検索</th>
      <th></th>
    </tr>
    <% @genres.each do |genre| %>
    <tr>
    <td><%= genre.name %></td>
    </tr>
    <% end %>
  
</table>

<table>
  <% @items.each do |item| %>
  <tr>
    <th>商品名</th>
    <td><%= item.name %></td>
  </tr>
  <tr>
    <th>値段</th>
    <td>¥<%= item.price %></td>
  </tr>
  <% end %>
</table>